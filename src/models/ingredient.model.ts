// export class Ingredient {
//   constructor(private ingredient: string, private quantity?: number, private unit?: string) {}
// }

export interface Ingredient {
  ingredient: string
  quantity: number
  unit: string
}